name: mb_button

description: A new button which requires only few arguments to get your desired button.

homepage: https://github.com/moulibheemaneti/mb_button.git

repository: https://github.com/moulibheemaneti/mb_button.git

version: 1.0.5

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
