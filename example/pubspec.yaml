name: example

description: A new Flutter project to demonstrate this package(mb_button).

publish_to: 'none'

version: 3.0.3+2

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  mb_button:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
